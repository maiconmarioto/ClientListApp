import React from "react";
import { View } from "react-native";
import Router from "./Router";
import { CustomHeaderSearch } from "./components/Header";

export default class App extends React.Component {
  render() {
    return <Router />;
  }
}
